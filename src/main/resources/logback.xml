<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

    <!-- Desabilitar logs de debug do gRPC -->
    <logger name="io.grpc" level="WARN" />
    <logger name="io.grpc.netty" level="WARN" />
    <logger name="io.grpc.netty.shaded" level="WARN" />
    <logger name="io.grpc.netty.shaded.io.grpc.netty" level="WARN" />
    <logger name="io.grpc.netty.shaded.io.grpc.netty.NettyServerHandler" level="WARN" />
    
    <!-- Desabilitar logs de debug do Netty -->
    <logger name="io.netty" level="WARN" />
    
    <!-- Manter logs da aplicação em INFO -->
    <logger name="br.com" level="INFO" />
</configuration>